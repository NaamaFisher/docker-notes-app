# Docker Notes App (Optimized)

A simple Flask-based note-taking application containerized with Docker.

## 🚀 Optimized Features:
- **Multi-Stage Build** for smaller image size
- **Health Check** to ensure container stability
- **.dockerignore** to remove unnecessary files

## 🛠 Build Instructions:

```sh
docker build -t docker-notes-app .

